name: ارسال فایل به روبیکا (API رسمی)

on:
  workflow_dispatch:
    inputs:
      link:
        description: 'لینک مستقیم فایل'
        required: true

jobs:
  send:
    runs-on: ubuntu-latest
    steps:
      - name: دانلود فایل از لینک
        env:
          URL: ${{ github.event.inputs.link }}
        run: |
          FILENAME=$(basename "$URL" | cut -d '?' -f1)
          curl -L -o "$FILENAME" "$URL"
          echo "FILENAME=$FILENAME" >> $GITHUB_ENV

      - name: ارسال فایل به روبیکا با API رسمی
        env:
          TOKEN: ${{ secrets.RUBIKA_BOT_TOKEN }}
          CHAT_ID: ${{ secrets.RUBIKA_CHAT_ID }}
        run: |
          curl -X POST "https://botapi.rubika.ir/v3/${TOKEN}/sendDocument" \
            -F "chat_id=${CHAT_ID}" \
            -F "document=@${FILENAME}"
